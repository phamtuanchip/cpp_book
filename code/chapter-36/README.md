# Chương 36 — Phụ lục (cheat sheet, tổng hợp lỗi thường gặp)

Phụ lục không có ví dụ mã nguồn mới: toàn bộ bảng tra cứu trong chương trỏ về các ví dụ
đã có ở `code/chapter-01` … `code/chapter-35`.

Thư mục này tồn tại để front-matter `code: code/chapter-36` của chương luôn hợp lệ với
`node tools/build.js check` (và để git theo dõi được thư mục — git không lưu thư mục rỗng).
